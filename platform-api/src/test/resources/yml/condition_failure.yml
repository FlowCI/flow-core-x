flow:
  - envs:
      FLOW_WORKSPACE: "echo hello"
      FLOW_VERSION: "echo version"

    steps:
      - name: step1
        condition: |
          println(FLOW_WORKSPACE)
          return false
        isAllowFailure: false
        script: "echo 1"

      - name: step2
        isAllowFailure: false
        script: "echo 2"
